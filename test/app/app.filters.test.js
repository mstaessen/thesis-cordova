describe('App Filters', function () {
    /*
    it('reverse should inverse strings', inject(function ($filter) {
        expect($filter('reverse')('hello')).toEqual('olleh');
    }));
    it('uppercase should convert strings to all caps', inject(function ($filter) {
        expect($filter('uppercase')('hello')).toEqual('HELLO');
    }));
    */
});